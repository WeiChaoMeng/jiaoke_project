<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jiaoke.oa.dao.OaOperateOfferMapper">

    <select id="searchFilter" parameterType="java.lang.String" resultType="com.jiaoke.oa.bean.OaOperateOffer">
        SELECT
            id,
            numbering,
            customer_name as customerName,
            project_name as projectName,
            salesman,
            quote_date as quoteDate,
            rich_text as richText,
            create_date as createDate
        FROM oa_operate_offer
        WHERE
            project_name LIKE "%"#{projectName}"%"
    </select>

    <select id="selectAllData" resultType="com.jiaoke.oa.bean.OaOperateOffer">
        SELECT
            id,
            numbering,
            customer_name as customerName,
            project_name as projectName,
            salesman,
            quote_date as quoteDate,
            rich_text as richText,
            create_date as createDate
        FROM oa_operate_offer
        order by
        create_date desc
    </select>
</mapper>